import { find, keys } from 'lodash';

export const SELECTED_ACTIVE = 'active';
export const SELECTED_INDETERMINATE = 'indeterminate';
export const SELECTED_OVERRIDDEN = 'overridden';

export default class Store {
}
